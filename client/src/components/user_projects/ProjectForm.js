import React from "react";

const ProjectForm = props => {

    const handleSubmit = () => {
        
    }

    return(
        <div className="grid">
            <form onSubmit={handleSubmit}>

            </form>
            Hello from ProjectForm
        </div>
    )
}

export default ProjectForm